{
  "name": "webpush-client",
  "version": "0.5.7",
  "description": "Web push client",
  "main": "dist/index.min.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy && npm run minify",
    "copy": "cpy src/webpush-sw.js dist",
    "minify": "terser -c -m -o dist/index.min.js -- dist/index.js && terser -c -m -o dist/webpush-client.min.js -- dist/webpush-client.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpy-cli": "^3.1.1",
    "terser": "^5.7.2",
    "typescript": "^4.3.5"
  },
  "files": [
    "dist"
  ]
}